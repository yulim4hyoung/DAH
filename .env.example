# SENTINEL 환경변수 템플릿
# 실제 키는 이 파일이 아니라 OS 환경변수(권장) 또는 복사본 .env(gitignore됨)에 넣는다.
#
# 권장: Windows 사용자 환경변수로 설정(파일에 안 남음)
#   setx ANTHROPIC_API_KEY "sk-ant-..."
#
# 대안: 이 파일을 .env 로 복사 후 아래 채우기 (절대 커밋/제출 금지)
ANTHROPIC_API_KEY=
# 선택: 보고서 근거자료 저장 폴더(미설정 시 results/ 만 사용)
SENTINEL_DOCS_DIR=
