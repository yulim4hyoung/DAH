# SENTINEL 의존성 (Python 3.12)
# PyTorch는 CPU 빌드로 충분(모델이 작아 수 분 내 학습). GPU 사용 시 아래 torch를
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# 로 교체(RTX 50xx=Blackwell은 cu128/nightly 필요할 수 있음).
torch==2.12.1
numpy==2.5.1
pandas==3.0.3
matplotlib==3.11.0
scikit-learn==1.9.0
anthropic==0.116.0
rich==15.0.0
pyyaml==6.0.3
